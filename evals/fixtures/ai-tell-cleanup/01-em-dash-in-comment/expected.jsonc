{
  // Three em dashes: two in block comments, one in a string literal.
  // String literal em dash is also flagged — it appears in an exception message
  // that will be user-visible, and em dash is an AI writing tell even in strings.
  "must_flag": [
    {
      "line": 8,
      "category": ["em-dash"],
      "hint": "em dash in inline comment — replace with ':' or rewrite"
    },
    {
      "line": 11,
      "category": ["em-dash"],
      "hint": "em dash in inline comment — replace with ':'"
    },
    {
      "line": 13,
      "category": ["em-dash"],
      "hint": "em dash in exception message string"
    }
  ],
  "must_not_flag": [],
  "notes": "All three em dashes are fix-severity. Auto-replace is safe for comments; string replacement should be proposed to the user."
}
